let x = 10;
let list_test = [1, 2, 3];
let list_mixed = [1, 2, "test"];
let string_test = "Hello, world!";

if (x >= 5) {
    return x == 10;
} else {
    for (x in list_test) {
        x = 5;
        return test(x);
    }
    while (x > 5) {
        return test(x);
    }

}

function test(num x) {
    return x == 5;
}