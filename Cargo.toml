[package]
name = "sagelang"
version = "0.1.0"
edition = "2024"

[dependencies]
colored = "3.0.0"

[target.'cfg(target_os = "macos")'.dependencies]
metal = "0.29"
objc = "0.2"

[target.'cfg(all(unix, not(target_os = "macos")))'.dependencies]
cuda-driver-sys = "0.3"

